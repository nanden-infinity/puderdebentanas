export default class Header {
  constructor(container, element) {
    this.container = container;
    this.element =  element;
  }

  getAllEelements(){
    const sectionHero = this.container.querySelector(".container--globel")
  }

  setModal(items){
    const modalElement = document.querySelector('.modal')?.classList.toggle('hidden')
    const items = { key, name} = objItems
  }
}
